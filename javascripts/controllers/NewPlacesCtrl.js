app.controller("NewPlacesCtrl", ($scope) => {
console.log("inside the NewPlacesCtrl");
///this is the search view will mimic the visual return of the "cards" with the static map, name, phone# ... 

});